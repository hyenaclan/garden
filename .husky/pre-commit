echo "🔍 Running pre-commit checks..."

# Run tests
echo "📝 Running tests..."
npm test || exit 1

# Run linting
echo "🎨 Running linter..."
npm run lint || exit 1

# Generate OpenAPI spec (optional - only if script exists)
if npm run | grep -q "^ *generate-openapi$"; then
  echo "📄 Generating OpenAPI spec..."
  if npm run generate-openapi 2>/dev/null; then
    git add apps/api/openapi.json 2>/dev/null
    echo "✅ OpenAPI spec generated and staged"
  else
    echo "⚠️  OpenAPI generation skipped (not available on this branch)"
  fi
fi

echo "✅ All pre-commit checks passed!"
